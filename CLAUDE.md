# CLAUDE.md

このファイルは、このリポジトリで Claude Code が作業する際のガイドラインです。

## プロジェクト概要

`taskboard.html` 1ファイルで完結する、ローカル完結型のタスクボード(担当者別ガントチャート)アプリです。

- 外部通信・外部ライブラリ・CDN依存は一切なし。ブラウザで直接開いて使う。
- データはブラウザの localStorage にのみ保存される（サーバーやビルド工程は存在しない）。
- ビルド／インストール手順は不要。`taskboard.html` をダブルクリックすれば動作する。

## Git運用ルール

- **コードを変更するたびに、GitHubにプッシュすること。**
  - `taskboard.html` を編集したら、都度コミットを作成し、`origin main` にプッシュする。
  - コミットは小さく、変更内容が分かるメッセージを付ける。
  - 作業を溜め込んで最後にまとめてプッシュしない。
